@inject IDialogService DialogService

<AppDialog Animation="Animation">
    <ChildContent>
                <h3>Test Dialog</h3>
                <input @bind="Name" placeholder="Enter name..." />
    </ChildContent>
    <Footer>
        <button @onclick="Save">Save</button>
        <button @onclick="Cancel">Cancel</button>
    </Footer>
</AppDialog>

@code {
    private string Name = string.Empty;

    [Parameter] public AppDialog.DialogAnimation Animation { get; set; }

    void Save()
    {
        DialogService.Close(Name);
    }

    void Cancel()
    {
        DialogService.Close<string>(null);
    }
}